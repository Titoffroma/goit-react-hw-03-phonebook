import styled from 'styled-components';

const Contacts = styled.ul`
  list-style-type: none;
`;

const ContactsListitem = styled.li`
  font-weight: bold;
  font-size: 14px;
`;

export { Contacts, ContactsListitem };
